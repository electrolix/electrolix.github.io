const search=document.getElementById("search"),filter=document.getElementById("filter"),cards=document.querySelectorAll(".card");function updateCards(){const e=search.value.toLowerCase().trim(),t=filter.value;cards.forEach(a=>{const r=a.innerText.toLowerCase(),d=a.dataset.category,c=r.includes(e),n="all"===t||d===t;a.style.display=c&&n?"block":"none"})}search.addEventListener("keyup",updateCards),filter.addEventListener("change",updateCards);
